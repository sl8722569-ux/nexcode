# NEXCODE — INSAN CREATIONS

Cross-device editor (Early Access).

Original Gemini HTML draft is in `original/` (prompt leaked into the UI; file was truncated). This repo’s `/web` app is the working rebuild.

## Run
https://sl8722569-ux.github.io/nexcode/web/

Local: open `web/index.html` or `python -m http.server` in `web/`.

## Honest limits
No real Git remote, no real phone pairing, no API keys in the page. SpaceXAI belongs on a backend later.
