Gemini draft problems (kept for the record):

1. User prompt leaked into the Open File shortcut row:
   `+  new project it is, refine it as hard as u can and also GEMINI did somethinh wronh, if, then chnage it`
2. File was truncated — no JavaScript, incomplete HTML (`rounded b`).
3. All onclick handlers (`setDesktopTab`, `openFileInEditor`, `toggleCommandPalette`, …) were undefined.
4. Copied Visual Studio Code logo SVG.
5. Fake iPhone 15 Pro handoff presented as if a real device was paired.

The Early Access app in `/web` fixes those.
